#include "main.h"

/**
 * _isdigit - checks for a digit
 * @c: character to check
 * Return: 1 if c is a digit, 0 otherwise
 */

int _isdigit(int c)
{
	int a;
	int b;

	b = 0;

	a = 9;

	if (c >= b || c <= a)

		return (1);
	else
		return (0);
}
