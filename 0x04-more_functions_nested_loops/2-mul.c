#include "main.h"
#include <stdio.h>

/**
 * mul -  multiplies two integers
 * @a: firt integer
 * @b: second integer
 * Return: Always 0
 */

int mul(int a, int b)
{
	int a = 2;
	int b = 3;

	_putchar("%d\n", 2, 3, mul(a, b));

	return (0);
}
